버전: 1.0.9

✨ 새로운 기능
- 메인 캘린더 날짜 셀에 '오늘 한 일' 활동 이모지 표시 (감정 이모지 아래, 개수에 따라 크기 조정)

🛠 개선 사항
* UI 색상 상수(`AppColors`) 및 공통 스낵바 헬퍼(`showFloatingSnackBar`) 추출로 코드 중복 제거
* 설정 권한 파일 간소화 (와일드카드 통합으로 44개 → 20개)
* `CLAUDE.md`에 화면 구성 및 전환 흐름 섹션 추가

🐛 버그 수정
* 없음
